# 服务器A的完整地址（您的 claude-code-hub 主服务）
UPSTREAM_SERVER_A=http://服务器A的IP:端口

# 端口设置（服务器B监听端口）
PORT=8000

# 超时设置（秒）
CONNECT_TIMEOUT_SEC=20
UPSTREAM_TIMEOUT_SEC=300

# 调试日志（1=开启，0=关闭）
DEBUG_LOG=1
